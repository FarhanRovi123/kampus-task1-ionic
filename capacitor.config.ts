import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tugasionic_install',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
